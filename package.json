{
    "name": "pokie-examples",
    "private": true,
    "version": "0.0.0",
    "type": "module",
    "pokie": {
        "entry": "./src/studio-fixture-game.js"
    },
    "scripts": {
        "start": "vite",
        "dev": "vite",
        "build": "tsc && vite build",
        "preview": "vite preview",
        "pages": "npm run build && gh-pages -d dist"
    },
    "devDependencies": {
        "gh-pages": "^5.0.0",
        "prettier": "^2.8.8",
        "typescript": "^5.0.2",
        "vite": "^4.3.9"
    },
    "dependencies": {
        "pokie": "^1.3.0"
    }
}
